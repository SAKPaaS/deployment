apiVersion: v1
kind: Service
metadata:
  name: backend-management-service
spec:
  selector:
    app: backend
  type: NodePort
  ports:
  - protocol: TCP
    port: 9000
    protocol: TCP
    targetPort: 9000
