apiVersion: v1
kind: Service
metadata:
  name: backend-integration
  labels:
    app: backend-integration
    environment: integration
spec:
  selector:
    app: backend-integration
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    protocol: TCP
    targetPort: 8080
